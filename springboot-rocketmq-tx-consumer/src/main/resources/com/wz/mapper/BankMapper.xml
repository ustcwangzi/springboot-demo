<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wz.mapper.BankMapper">
  <resultMap id="BaseResultMap" type="com.wz.model.Bank" >
    <id column="userid" property="userid" jdbcType="VARCHAR" />
    <result column="username" property="username" jdbcType="VARCHAR" />
    <result column="amount" property="amount" jdbcType="DOUBLE" />
    <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
  </resultMap>

  <select id="queryById" resultMap="BaseResultMap">
    select userid,username,amount,update_time from bank where userid = #{userid}
  </select>

  <insert id="insert" parameterType="com.wz.model.Bank" >
    insert into bank (userid,username,amount)
    values (#{bank.userid,jdbcType=VARCHAR}, #{bank.username,jdbcType=VARCHAR}, #{bank.amount,jdbcType=DOUBLE})
  </insert>

  <update id="updateAmount">
    update bank set amount = #{amount} where userid = #{userid}
  </update>
</mapper>